function [M,N] = MN4pfcord(param)
H=length(param)-1;
S = param{H+1}.sigmag;
mu = param{H+1}.mu;
for h=1:H,
    Sfit = Sfit + (param{h}.n*(param{h}.mu - mu)'*(param{h}.mu - mu)

M = Sfit;
N = S;
